---
description: Create a git commit with a conventional commit message (analyze diff, stage, commit).
---

Load the `git-commit` skill and follow it to create a commit from the current
changes: analyze the diff, stage files in logical groups, generate a
conventional commit message, and run `git commit`.

User input (optional type/scope/description overrides): $ARGUMENTS
